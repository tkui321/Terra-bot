{
  "commands": {
    "cmd_eval": {
		"__COMMENT": "add stuff to the replace for the eval to replace! (cool right?)",
      "replace": [
        [";eval", ""],
        ["\n", ""],
		    ["print(", "message.channel.send("]
      ]
    }
  }
}
